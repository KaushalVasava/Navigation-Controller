<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
package="com.lasuak.navigationcontroller">

<application
    android:name=".project1.App"
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.NavigationController">
    <activity
        android:name=".project1.activity.MainActivity"
        android:exported="true">

        <nav-graph android:value="@navigation/main_nav_graph" />
    </activity>
    <activity
        android:name=".project2.MainActivity2"
        android:exported="true">
        <nav-graph android:value="@navigation/main_nav_graph2" />
    </activity>
    <activity
        android:name=".project3.MainActivity3"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
</application>
</manifest>